<%= form_for(@c_config_type) do |f| %>
  <% if @c_config_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c_config_type.errors.count, "error") %> prohibited this config_type from being saved:</h2>

      <ul>
      <% @c_config_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :klass %><br>
    <%= f.text_field :klass %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
