<%= form_for([@c_config_type, @c_config_value]) do |f| %>
    <% if @c_config_value.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@c_config_value.errors.count, "error") %> prohibited this config_value from being
                saved:</h2>

            <ul>
                <% @c_config_value.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :scope %><br>
        <%= f.text_field :scope %>
    </div>
    <div class="field">
        <%= f.label :value %><br>
        <%= f.text_area :value %>
    </div>

    <div>
        <%= f.hidden_field :config_type_id %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
